/**
 * Admin email — set in .env (local) and Vercel Environment Variables (production).
 * No default; must be set for admin login to work.
 */
export const ADMIN_EMAIL = process.env.ADMIN_EMAIL ?? "";

